#!/usr/bin/env python3

#HALP Commit with the given args as message.

from common import run, args

def main():
    run(['git', 'commit', '-m', ' '.join(args())])

if '__main__' == __name__:
    main()
